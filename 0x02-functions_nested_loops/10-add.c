#include <stdio.h>
#include "main.h"
/**
 * add - a function to add two integers based on two parameters
 * @a: first integer.
 * @b: second integer.
 * Return: Always an int.
 */
int add(int a, int b)
{
	return(a + b);
}
