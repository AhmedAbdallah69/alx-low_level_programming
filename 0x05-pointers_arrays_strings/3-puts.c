#include <stdio.h>
#include "main.h"
/**
 * _puts - a function that prints a string similar to 'puts'.
 * @str: string to be acted upon.
 * Return: Always void.
 */
void _puts(char *str)
{
	int i;

	for (i = 0; s[i] != 0; i++)
		putchar(s[i]);
}
